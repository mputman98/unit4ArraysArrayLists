

/**
 * Write a description of class ArrayOps2 here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class ArrayOps2
{
    /** description of instance variable x (add comment for each instance variable) */
    private int x;

    /**
     * Default constructor for objects of class ArrayOps2
     */
    private int[] values;
    public ArrayOps2(int[] initialValues){
        values = initialValues;
    }
    public void swapFandL(){
        int x;
        int x = values[values.length];
        values[values.length] = values[0] 
        values[0] = int x;
    }
    public void shiftRight(){
        for (int num = 0; num < x.length; num++){
            if (num != 0 || x.length){
                x[num] = x[num+1] 
            
        
        
    